<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:Mercury.Editor.Controls"
        xmlns:localization="clr-namespace:Mercury.Editor.Localization"
        xmlns:dataTemplates="clr-namespace:Mercury.Editor.Converters.DataTemplates">
    <Design.PreviewWith>
        <controls:ChapterControl />
    </Design.PreviewWith>

    <Style Selector="controls|ChapterControl">
        <!-- Set Defaults -->
        <Setter Property="Template">
            <ControlTemplate>
                <Grid RowDefinitions="Auto,*">
                    <Grid.Styles>
                        <StyleInclude Source="/Assets/Styles/GuideStyles.axaml"></StyleInclude>
                    </Grid.Styles>
                    <Button Grid.Row="0" Command="{TemplateBinding GoBack}"
                            Margin="10,0"
                            Content="{Binding Path=GoBackText, Source={x:Static localization:GuideResources.Instance}}"/>
                    <ContentControl Grid.Row="1" Content="{TemplateBinding CurrentGuide}">
                        <ContentControl.DataTemplates>
                            <dataTemplates:GuideChapterDataTemplate></dataTemplates:GuideChapterDataTemplate>
                        </ContentControl.DataTemplates>
                    </ContentControl>
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
</Styles>
